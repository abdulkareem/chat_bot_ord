{
  "name": "hyperlocal-chat-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "CI=1 wrangler deploy",
    "test": "node --test",
    "lint": "echo 'No linter configured'",
    "db:init": "node scripts/init-db.js",
    "db:audit": "node scripts/audit-sync.js",
    "db:sync": "node scripts/audit-sync.js --apply"
  },
  "dependencies": {
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "wrangler": "^4.9.0"
  }
}
