{
  "name": "hyperlocal-chat-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "wrangler": "^4.9.0"
  }
}
